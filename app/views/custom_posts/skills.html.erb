<div data-controller="work" class="relative flex flex-col justify-center items-center gap-10 mx-auto mt-6 p-6 sm:p-10 rounded-2xl w-full h-full font-sans">
  <h2 data-work-target="fade" class="opacity-0 mb-10 sm:mb-14 font-mono font-bold text-2xl sm:text-3xl text-center transition-all translate-y-6 duration-700">
    Entre Código y Pruebas: Mi Skillset 💻🔧
  </h2>
  <div data-work-target="fade" class="flex flex-col gap-6 sm:gap-8 opacity-0 mb-32 sm:mb-[170px] px-2 sm:px-0 w-full max-w-3xl h-full text-base text-left leading-relaxed transition-all translate-y-6 duration-700">
    <%= render partial: 'custom_posts/skilltext' %>
    <h3 data-work-target="fade" class="opacity-0 mt-10 mb-20 sm:mb-[100px] font-mono text-xl sm:text-2xl text-center transition-all translate-y-6 duration-700">
      Tecnologías y herramientas que utilizo
    </h3>
    <% @skills["skills"].each do |category, skills| %>
      <h4 data-work-target="fade" class="mt-2 mb-2 font-mono font-bold sm:text-md text-sm text-center"><%= category.humanize %></h4>
      <% skills.each do |skill| %>
        <div data-work-target="fade" class="flex justify-center">
          <%= render partial: "custom_posts/skillcard", locals: { name: skill["name"], iconurl: skill["iconurl"], category: category, description: skill["description"] } %>
        </div>
      <% end %>
    <% end %>
    <div class="flex flex-col justify-center items-center mt-6">
      <%= render partial: "posts/return" %>
    </div>
  </div>
</div>
